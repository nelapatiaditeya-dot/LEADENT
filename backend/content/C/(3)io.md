Programs often need to take input from the user and display output.
Output
To display something on the screen, we use:
printf("Hello");

Input
To take input from the user, we use:
scanf("%d", &num);

Example:
int num;
scanf("%d", &num);
printf("You entered: %d", num);
